# packages allow you to group multiple related modules and sub packages and bring them all into a file
# through a single import statement. A package is essentially a reusable directory, an importable directory
# of modules. Giving you access to all the modules within the package.

# Packages enhance code organization in a hierarchical manner, promote code reuse, and encapsulation.

# The __init__.py file is what indicates that a directory is a package.

# pip is Pythons standard package manager, its used for installing, upgrading, and managing python packages
# from the Python Package Index PyPI (similar to npm for JS)

# Use pip to install 3rd party packages form PyPI, then import them into your scripts
# We can use an alias to make verbose package names more concise and usable.
import numpy as np