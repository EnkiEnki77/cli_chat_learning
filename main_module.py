# The name of each module is always available in the __name__ variable
# When you execute any script its __name__ variable is always set to __main__

import test_module

print("Hello from main_module.py, my __name__ is", __name__)